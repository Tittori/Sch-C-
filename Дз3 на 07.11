a = int(input())
b = int(input())
    for num in range(a,b):
        count = 0
        divisors = []
        for i in range(1, num + 1):
              if num % i == 0
                     count + = 1
                     divisors.append(i)
              if count > 4:
                      break
        if count == 4:
                      divisors.sort(reverse = True)
                      print (‘’{num} имеет 4 делителя: {divisors}’’)
       
